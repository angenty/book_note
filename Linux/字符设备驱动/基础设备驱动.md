# 基础设备驱动

## 简介

Linux 应用程序对驱动程序的调用

![Linux应用程序调用过程|515](Linux应用程序调用过程.png)

通过系统调用“陷入”到内核空间，所谓陷入其实也是一种异常中断，只不过是比较特殊的异常，专门用于用户态切换为内核态

在 Linux 内核文件 include/linux/fs.h 中有个叫做 file_operations 的结构体，此结构体就是 Linux 内核驱动操作函数集合

```c
struct file_operations
```

**在驱动程序中获取设备树信息的情况**

| 阶段   | 发生了什么                          | 你的程序需要做什么      |
| ---- | ------------------------------ | -------------- |
| 启动早期 | Bootloader从Flash读取DTB到内存       | 什么都不做          |
| 内核启动 | 内核解析DTB，创建内存数据结构               | 什么都不做          |
| 运行时  | 你的驱动调用`of_find_node_by_path()` | 直接查内存，不涉及Flash |
==在驱动代码中我使用获取设备树函数的时候，设备树已经被解析进内存了，程序是在内存中查找到设备树的信息的==

**设备树的存储**：编译成dtb文件后，作为一个独立文件存储在特定的flash地址上

## 字符设备驱动开发步骤

==在 Linux 驱动开发中我们需要按照其规定的框架来编写驱动，所以说学 Linux 驱动开发重点是学习其驱动框架==

==模块的编写和裸机时编写模块驱动没区别，想想自己在实习的时候写的LCD驱动模块，都是通过注册回调达到模块化的效果，不过Linux有更完善的模块框架==
### 驱动模块的加载和卸载

**Linux 驱动有两种运行方式**：

- 第一种就是将驱动编译进 Linux 内核中，这样当 Linux 内核启动的时候就会自动运行驱动程序。
- 第二种就是将驱动编译成模块(Linux 下模块扩展名为.ko)，在Linux 内核启动以后使用“insmod”命令加载驱动模块。

调试的时候用第二种

**加载和卸载命令**

| 命令          | 说明                                                                                     | 例子                 |
| ----------- | -------------------------------------------------------------------------------------- | ------------------ |
| insmod      | 是最简单的模块加载命令，此命令用于加载指定的.ko 模块                                                           | insmod drv.ko      |
| modprobe    | 会分析模块的依赖关系，然后会将所有的依赖模块都加载到内核中                                                          | modprobe drv.ko    |
| rmmod       | 驱动模块的卸载                                                                                | rmmod drv.ko       |
| modprobe -r | 卸载驱动命令，使用 modprobe 命令可以卸载掉驱动模块所依赖的其他模块，前提是这些依赖模块已经没有被其他模块所使用，否则就不能使用 modprobe 来卸载驱动模块。 | modprobe -r drv.ko |

### 申请设备号

具体申请详看下面的笔记[Linux 设备号](## Linux设备号)

### 字符设备注册与注销

==所谓注册设备，就是物理设备号和操作函数进行绑定，然后写进系统的设备管理表内，里面记录着逻辑设备、物理设备、驱动入口地址一 一对应的关系==

字符设备的注册和注销函数原型如下所示:

```c
static inline int register_chrdev(unsigned int major, const char *name,const struct file_operations *fops)
```

**参数**：
- major：主设备号，Linux 下每个设备都有一个设备号，设备号分为主设备号和次设备号两部分
- name：设备名字，指向一串字符串
- fops：结构体 file_operations 类型指针，指向设备的操作函数集合变量

```c
static inline void unregister_chrdev(unsigned int major, const char *name)
```

**参数**：
- major：要注销的设备对应的主设备号
- name：要注销的设备对应的设备名

还有另一种注册函数：
```c
/*参数 from 是要申请的起始设备号，也就是给定的设备号；参数 count 是要申请的数量，一般都是一个；参数 name 是设备名字。*/
int register_chrdev_region(dev_t from, unsigned count, const char *name)
```

### 创建设备节点文件

具体详看下面的笔记

所谓创建节点文件，即创建一个逻辑设备和物理设备和设备驱动一一对应

类似于

| 逻辑设备名    | 物理设备名 | 驱动程序入口地址 |
| -------- | ----- | -------- |
| /dev/tty | 5     | 2046     |
系统设备表

| 逻辑设备名    | 系统设备指针 |
| -------- | ------ |
| /dev/tty | 3      |
用户设备表

这是两种不同存储方式，具体看操作系统怎么设计的
### 实现设备的具体操作函数

这就是具体驱动编写了，和裸机程序编写本质上没有区别，自己根据实际情况编写

### 添加 LICENSE 和作者信息

最后我们需要在驱动中加入 LICENSE 信息和作者信息，其中 LICENSE 是必须添加的，否则的话编译的时候会报错，作者信息可以添加也可以不添加。

```c
MODULE_LICENSE()  //添加模块 LICENSE 信息
MODULE_AUTHOR()  //添加模块作者信息
```

### 杂记

因为内核空间不能直接操作用户空间的内存，因此需要借助 copy_to_user 函数来完成内核空间的数据到用户空间的复制。copy_to_user 函数原型如下：

```c
static inline long copy_to_user(void __user *to, const void *from, unsigned long n)
```

参数 to 表示目的，参数 from 表示源，参数 n 表示要复制的数据长度。如果复制成功，返回值为 0，如果复制失败则返回负数。

## Linux设备号

### 设备号的组成

Linux 中每个设备都有一个设备号，设备号由主设备号和次设备号两部分组成，主设备号表示==某一个具体的驱动==，次设备号表示==使用这个驱动的各个设备==。

Linux 提供了一个名为 dev_t 的数据类型表示设备号，dev_t 定义在文件 include/linux/types.h 里面，定义如下：

```c
typedef __u32 __kernel_dev_t;
typedef __kernel_dev_t dev_t;
```
可以看出 dev_t 是__u32 类型的

这 32 位的数据构成了主设备号和次设备号两部分，其中高 12 位为主设备号，低 20 位为次设备号。因此主设备号范围为 0~4095

### 设备号的分配

**静态分配设备号**

也就是自己指定一个设备号，需要自己使用“cat /proc/devices”命令即可查看当前系统中所有已经使用了的设备号，然后指定一个没有使用的设备号，比较麻烦

**动态分配设备号**

也就是用程序自动申请一个没有使用的设备号，简便易管理

设备号的申请函数如下：

```c
int alloc_chrdev_region(dev_t *dev, unsigned baseminor, unsigned count, const char *name)
```

**参数**：
- dev：保存申请到的设备号
- baseminor：次设备号起始地址，alloc_chrdev_region 可以申请一段连续的多个设备号，这些设备号的主设备号一样，但是次设备号不同，次设备号以 baseminor 为起始地址地址开始递增。一般 baseminor 为 0，也就是说次设备号从 0 开始。
- count：要申请的设备号数量
- name：设备名字

设备号释放函数如下：

```c
void unregister_chrdev_region(dev_t from, unsigned count)
```

**参数**：
- from：要释放的设备号
- count：表示从 from 开始，要释放的设备号数量

## printk函数

printk 相当于 printf 的孪生兄妹，printf运行在用户态，printk 运行在内核态。在内核中想要向控制台输出或显示一些内容，必须使用printk 这个函数。

不同之处在于，printk 可以根据日志级别对消息进行分类，一共有 8 个消息级别，这 8 个消息级别定义在文件 include/linux/kern_levels.h 里面，定义如下：

```c
#define KERN_SOH "\001"
#define KERN_EMERG KERN_SOH "0" /* 紧急事件，一般是内核崩溃 */
#define KERN_ALERT KERN_SOH "1" /* 必须立即采取行动 */
#define KERN_CRIT KERN_SOH "2" /* 临界条件，比如严重的软件或硬件错误*/
#define KERN_ERR KERN_SOH "3" /* 错误状态，一般设备驱动程序中使用KERN_ERR 报告硬件错误 */
#define KERN_WARNING KERN_SOH "4" /* 警告信息，不会对系统造成严重影响 */
#define KERN_NOTICE KERN_SOH "5" /* 有必要进行提示的一些信息 */
#define KERN_INFO KERN_SOH "6" /* 提示性的信息 */
#define KERN_DEBUG KERN_SOH "7" /* 调试信息 */
```

一共定义了 8 个级别，其中 0 的优先级最高，7 的优先级最低。如果要设置消息级别，参
考如下示例：

```c
printk(KERN_EMERG "gsmi: Log Shutdown Reason\n");
```

上述代码就是设置“gsmi: Log Shutdown Reason\n”这行消息的级别为 KERN_EMERG。在具体的消息前面加上 KERN_EMERG 就可以将这条消息的级别设置为 KERN_EMERG。

如果使用printk的时候不显式的设置消息级别 ，那么printk将会采用默认级别MESSAGE_LOGLEVEL_DEFAULT，MESSAGE_LOGLEVEL_DEFAULT 默认为 4。

在 include/linux/printk.h 中有个宏 CONSOLE_LOGLEVEL_DEFAULT，定义如下：
```c
#define CONSOLE_LOGLEVEL_DEFAULT 7
```
CONSOLE_LOGLEVEL_DEFAULT 控制着哪些级别的消息可以显示在控制台上，此宏默认为 7，意味着只有优先级高于 7 的消息才能显示在控制台上。

## 地址映射

如果没有开启 MMU 的话直接向 0X020E0068 这个寄存器地址写入数据就可以配置 GPIO1_IO03 的复用功能。现在开启了 MMU，并且设置了内存映射，因此就不能直接向 0X020E0068 这个地址写入数据了。我们必须得到 0X020E0068 这个物理地址在 Linux 系统里面对应的虚拟地址，这里就涉及到了物理内存和虚拟内存之间的转换，需要用到两个函数：ioremap 和 iounmap。

==这两个函数的作用也就是通过一张表将输入的物理/虚拟地址转化成虚拟/物理地址输出==

**ioremap 函数**

```c
//arch/arm/include/asm/io.h

#define ioremap(cookie,size) __arm_ioremap((cookie), (size), MT_DEVICE)

void __iomem * __arm_ioremap(phys_addr_t phys_addr, size_t size, unsigned int mtype)
{
 return arch_ioremap_caller(phys_addr, size, mtype,__builtin_return_address(0));
}
```

**参数**：
- phys_addr：要映射的物理起始地址。
- size：要映射的内存空间大小。
- mtype：ioremap 的类型，可以选择 MT_DEVICE、MT_DEVICE_NONSHARED、MT_DEVICE_CACHED 和 MT_DEVICE_WC，ioremap 函数选择 MT_DEVICE。

**返回值**：
- iomem 类型的指针，指向映射后的虚拟空间首地址。

**应用例程**：

```c
#define SW_MUX_GPIO1_IO03_BASE (0X020E0068)
static void __iomem* SW_MUX_GPIO1_IO03;
SW_MUX_GPIO1_IO03 = ioremap(SW_MUX_GPIO1_IO03_BASE, 4);
```

**iounmap 函数**

```c
void iounmap (volatile void __iomem *addr)
```

**参数**：
- addr：要取消映射的虚拟地址空间首地址

**应用例程**：

```c
iounmap(SW_MUX_GPIO1_IO03);
```


**使用完ioremap 函数后，为什么还要使用iounmap 函数释放映射?**

核心原因：资源管理

```plain
ioremap()  →  建立虚拟地址映射（消耗资源：页表、TLB、内存）
    ↓
[使用期间]  →  通过虚拟地址访问硬件寄存器
    ↓
iounmap()  →  释放映射（回收资源）
```

| 资源类型                  | 不释放的后果         |
| --------------------- | -------------- |
| **页表（Page Table）**    | 持续占用，导致内存泄漏    |
| **TLB(快表)条目**         | 不刷新，影响MMU效率    |
| **虚拟地址空间**            | 内核空间耗尽，无法建立新映射 |
| **struct vm\_struct** | 内核数据结构泄漏       |
比如虚拟内存的逻辑内存块5号为空闲，使用ioremap映射这个IO寄存器所在外存地址，比如这个IO外存地址块号位100号，则建立映射关系

| 虚拟内存块号 | 外存块号 |
| ------ | ---- |
| 5      | 100  |
这样虚拟内存的块号5就被占用了

### I/O 内存访问函数

读操作函数

```c
u8 readb(const volatile void __iomem *addr)   //8bit读操作
u16 readw(const volatile void __iomem *addr)  //16bit读操作
u32 readl(const volatile void __iomem *addr)  //32bit读操作
```

写操作函数

```c
void writeb(u8 value, volatile void __iomem *addr)  //8bit写操作
void writew(u16 value, volatile void __iomem *addr) //16bit写操作
void writel(u32 value, volatile void __iomem *addr) //32bit写操作
```

## 新的字符设备注册方法

### 字符设备结构

```c
// include/linux/cdev.h
struct cdev {
    struct kobject kobj;           // 内核对象，用于sysfs/sys文件系统
    struct module *owner;          // 所属模块（THIS_MODULE），防止卸载时正在使用
    const struct file_operations *ops;  // 文件操作函数集（open/read/write/ioctl等）
    struct list_head list;         // 链接到全局字符设备链表
    dev_t dev;                     // 设备号（主设备号+次设备号）
    unsigned int count;            // 次设备号数量（连续分配的设备数）
};
```

在 cdev 中有两个重要的成员变量：ops 和 dev，这两个就是字符设备文件操作函数集合file_operations 以及设备号 dev_t。

**cdev_init 函数**

主要是清零和注册操作函数的操作

```c
void cdev_init(struct cdev *cdev, const struct file_operations *fops)
```

**参数**：
- cdev：是要初始化的 cdev 结构体变量
- fops：就是字符设备文件操作函数集合

==创建class，跟创建c++里的对象类似，对象为这个模块，功能为操作函数，属性为设备号这些变量==